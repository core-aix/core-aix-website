{
  "name": "core-aix-website",
  "private": true,
  "description": "Netlify functions that run alongside the Hugo site.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
